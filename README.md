# Installation

Install Webpack

```sh
npm install webpack -g
```

Install dependencies
```sh
npm install
```

# Run Application

```sh
webpack -w --progress
```
Open the index.html file in the browser.